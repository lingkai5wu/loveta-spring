<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.lingkai5wu.loveta.mapper.PermissionMapper">

    <select id="listPermissionsById" resultType="java.lang.String">
        SELECT DISTINCT auth_permission.code
        FROM auth_permission
                 INNER JOIN auth_group_permissions ON auth_permission.id = auth_group_permissions.permission_id
                 INNER JOIN auth_user_groups ON auth_group_permissions.group_id = auth_user_groups.group_id
        WHERE auth_user_groups.user_id = #{id}
    </select>
</mapper>
