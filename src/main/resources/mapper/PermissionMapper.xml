<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.lingkai5wu.loveta.mapper.PermissionMapper">

    <select id="listPermissionsByUserId" resultType="java.lang.String" parameterType="object">
        SELECT p.code
        FROM auth_permission p
                 INNER JOIN auth_group_permission gp ON p.id = gp.permission_id
                 INNER JOIN auth_group_user gu ON gp.group_id = gu.group_id
        WHERE gu.user_id = #{id}
    </select>
</mapper>
